package main.web.services.fitsense.catalog.domain.model.valueobjects;

/** Como se prescribe un ejercicio. Debe coincidir con ck_exercises_prescription. */
public enum PrescriptionType {
    SETS_REPS,
    DURATION
}
