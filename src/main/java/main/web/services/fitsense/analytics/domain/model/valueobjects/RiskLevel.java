package main.web.services.fitsense.analytics.domain.model.valueobjects;

/** Debe coincidir con ck_wum_risk_level. Los cortes viven en calculation_configs, no aqui. */
public enum RiskLevel {
    LOW,
    MODERATE,
    HIGH,
    CRITICAL
}
